{
  "name": "i18n-janitor",
  "version": "1.0",
  "description": "i18n-janitor is a CLI utility that finds and removes unused localization tokens",
  "bin": "./cli.mjs",
  "dependencies": {
    "jsonfile": "^5.0.0"
  }
}
